codes=$HOME/Codes
crepo=$codes/crepo
myrepo=$codes/myrepo
documents=$HOME/Documents
organize=$documents/org_anize

[[ -d "$codes" ]] || mkdir $codes
[[ -d "$crepo" ]] || mkdir $crepo
[[ -d "$myrepo" ]] || mkdir $myrepo
[[ -d "$documents" ]] || mkdir $documents
[[ -d "$organize" ]] || mkdir $organize
